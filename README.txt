RESTART ROUTE FIX

Copy/overwrite this file into your project:
app/api/games/[gameId]/restart/route.js

Then run locally:
npm run dev

Push to GitHub:
git add .
git commit -m "Add same-link restart API route"
git push

No Supabase SQL changes are required.
